#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 *
 * @s: input string
 *
 * Return: length
 */

int _strlen(char *s)
{
	int len;
	double size;
	char a = *s

	size = sizeof(a);
	len = 0;

	for ( ; len < size + 1; len++)

	return (len);

}
